apiVersion: v1
kind: Service
metadata:
  name: scalable-cache-api-service
spec:
  type: NodePort
  selector:
    app: scalable-cache-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30082